spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: sweetify
            name: ms-products
      reload:
        enabled: true
        strategy: restart_context
        mode: event